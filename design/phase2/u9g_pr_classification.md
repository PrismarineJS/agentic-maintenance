# U9G PR classification

Snapshot: 2026-09-14 UTC. Scope: the original 78-PR inventory associated with [mineflayer #4119](https://github.com/PrismarineJS/mineflayer/issues/4119). Nine have since merged, leaving 69 open. This is not a fresh census of every PR by U9G.

Revised after ten subagent audits, run with up to three workers alongside the primary agent. Three workers independently covered mineflayer, protocol/data/item, and viewer; the primary agent covered pathfinder/physics. Seven further workers checked disputed categories and merge/dependency claims. The primary agent reconciled the findings.

This remains static triage of current patches, surrounding code where needed, discussions and reported CI. We did not run these branches or independently reproduce every author trace/generated artifact. Category 1 means no blocker was found and recorded checks pass, not exhaustive correctness certification. Category 2 includes bounded code fixes, review questions, dependency releases, conflicts and unresolved CI. Category 3 requires substantial additional work; optional cleanup, a broad patch, or a small upstream extraction alone does not qualify. Category 4 rejects the proposed approach, not the underlying problem or contributor.

| Category | Count |
| --- | ---: |
| 1. Can be merged as is | 15 |
| 2. Leave comments / resolve focused blockers | 49 |
| 3. Requires substantial changes / refactoring | 2 |
| 4. Reject the current approach | 3 |
| **Total open** | **69** |

No GitHub comments, reviews or merges were submitted. Reasons below are proposed review points.

## Changes after independent review

| PR | Previous → revised |
| --- | --- |
| [PrismarineJS/mineflayer #4063](https://github.com/PrismarineJS/mineflayer/pull/4063) | 3 → 2 |
| [PrismarineJS/prismarine-item #184](https://github.com/PrismarineJS/prismarine-item/pull/184) | 3 → 2 |
| [PrismarineJS/mineflayer #4107](https://github.com/PrismarineJS/mineflayer/pull/4107) | 3 → 2 |
| [PrismarineJS/mineflayer #4115](https://github.com/PrismarineJS/mineflayer/pull/4115) | 3 → 2 |
| [PrismarineJS/mineflayer #4118](https://github.com/PrismarineJS/mineflayer/pull/4118) | 3 → 2 |
| [PrismarineJS/prismarine-viewer #490](https://github.com/PrismarineJS/prismarine-viewer/pull/490) | 2 → 1 |

The initial assessment overstated several architectural prerequisites and missed work already present in current revisions. Item #184 already introduces generic hashing and declarative component shapes. Mineflayer #4115 already documents fallback and tests real world raycasts; #4114 already tests serialization. Ownership questions and movement correctness issues deserve focused comments before being called major redesigns. Viewer #490 received the requested screenshot and passing CI.

## 1. Can be merged as is (15)

| PR | Head | Checks / merge state | Reason / next review point |
| --- | --- | --- | --- |
| [PrismarineJS/minecraft-data #1273](https://github.com/PrismarineJS/minecraft-data/pull/1273) — pc/protocol: entity_teleport layout for 1.21.2+ (velocity, f32 rotation, relative flags) | `30dc8ae4` | SUCCESS; MERGEABLE | Narrow wire-layout correction; the author answered the decompiled-source question with codec excerpts. Current checks pass. |
| [PrismarineJS/minecraft-data #1282](https://github.com/PrismarineJS/minecraft-data/pull/1282) — Add the useEntityHasLocation feature | `2427e5b1` | SUCCESS; MERGEABLE | Small feature flag for the new use_entity shape; useful independently of the downstream implementation. |
| [PrismarineJS/minecraft-data #1267](https://github.com/PrismarineJS/minecraft-data/pull/1267) — pc: fix intangible_projectile NBT and 26.1 item stack templates | `8b81a202` | SUCCESS; MERGEABLE | NBT/component and nested-stack corrections have specific decompiled codec evidence; current checks pass. |
| [PrismarineJS/mineflayer #4068](https://github.com/PrismarineJS/mineflayer/pull/4068) — fix: drop the physics tick backlog after an event-loop stall instead of draining it | `47b1b39a` | SUCCESS; MERGEABLE | Reviewed actual production patch and regression: modulo drops capped whole-tick backlog and preserves the fractional timestep; no concrete blocker found. Timing-test variability is a follow-up concern, not a demonstrated failure with current passing CI. |
| [PrismarineJS/mineflayer #4069](https://github.com/PrismarineJS/mineflayer/pull/4069) — fix: send held_item_slot only when the slot differs from the slot the server holds | `d21e8324` | SUCCESS; MERGEABLE | Reviewed held-slot handlers and simple-inventory integration. Tracks last outbound carried slot, resets on login, ignores invalid inbound hotbar slots and suppresses duplicate writes. Tests cover same-slot server updates and repeated login. No concrete regression established; the server-holds wording is less precise than last-sent tracking. |
| [PrismarineJS/mineflayer #4071](https://github.com/PrismarineJS/mineflayer/pull/4071) — fix: reset teams and scoreboards on every login | `07248e2c` | SUCCESS; MERGEABLE | Reviewed reset loops and surrounding scoreboard implementation. Login clears per-bot teams/objectives/member mappings and numeric display slots while preserving accessor aliases and public object identity. Respawn intentionally retains state. No concrete blocker found. |
| [PrismarineJS/mineflayer #4088](https://github.com/PrismarineJS/mineflayer/pull/4088) — internal tests: wait for plugin injection before driving the bot from playerJoin | `45ec7045` | SUCCESS; MERGEABLE | Reviewed test-only readiness waits against plugin_loader: inject_allowed invokes plugin injection synchronously before the newly registered promise resolver. The awaits therefore remove the mock-server race. No concrete blocker found. |
| [PrismarineJS/mineflayer #4100](https://github.com/PrismarineJS/mineflayer/pull/4100) — Make the named scoreboard display slots non-enumerable | `2938fa21` | SUCCESS; MERGEABLE | Reviewed non-enumerable accessor change against scoreboard display/removal handlers. Numeric slots remain enumerable and aliases resolve correctly; change is compatible with login reset #4071. Existing multiple-display-slot removal and empty-name clearing limitations predate this patch and do not establish a new blocker. |
| [PrismarineJS/mineflayer-pathfinder #378](https://github.com/PrismarineJS/mineflayer-pathfinder/pull/378) — createHuman: take three-block drops | `ea588494` | SUCCESS; MERGEABLE | Localized correction for maxDropDown counting the landing block, with a regression for the intended three-block fall; checks pass. |
| [PrismarineJS/mineflayer-pathfinder #380](https://github.com/PrismarineJS/mineflayer-pathfinder/pull/380) — createHuman: a walk that stops under an unreachable goal rejects instead of resolving | `3191bf0e` | SUCCESS; MERGEABLE | Narrow vertical-proximity guard prevents false success beneath unreachable goals; targeted regression coverage and passing checks. |
| [PrismarineJS/mineflayer-pathfinder #381](https://github.com/PrismarineJS/mineflayer-pathfinder/pull/381) — createHuman: walkTo and lookAt say so when physics is disabled | `c1cc510b` | SUCCESS; MERGEABLE | Clear upfront errors when physics is disabled, with diagnostics for a mid-walk timeout. Small documented behavior change with passing checks. |
| [PrismarineJS/node-minecraft-protocol #1523](https://github.com/PrismarineJS/node-minecraft-protocol/pull/1523) — fix: write the 0 holder id for inline registryEntryHolder entries | `3e4dea27` | SUCCESS; MERGEABLE | Writes the previously uninitialized inline-holder discriminator. The 0xff-filled buffer regression detects the skipped byte; the discussion explains why encoding zero writes the required byte. Checks pass. |
| [PrismarineJS/node-minecraft-protocol #1524](https://github.com/PrismarineJS/node-minecraft-protocol/pull/1524) — test: retry the vanilla ping while the server has no status yet | `cd762d7b` | SUCCESS; MERGEABLE | Test-only bounded retry for a specific vanilla startup race, preserving failure for unrelated errors; checks pass. |
| [ProtoDef-io/node-protodef #178](https://github.com/ProtoDef-io/node-protodef/pull/178) — FullPacketParser: emit partialReadError with the chunk it could not read | `0087395b` | SUCCESS; MERGEABLE | Reviewed event emission, typing and interpreted/compiled regression tests. Adds chunk context and a non-error event to the existing partial-read recovery path without changing normal parsing; tests establish continued parsing. Documentation could distinguish partial-read errors more narrowly, but no code blocker found. |
| [PrismarineJS/prismarine-viewer #490](https://github.com/PrismarineJS/prismarine-viewer/pull/490) — Render player skins | `832cb6d4` | SUCCESS; MERGEABLE | Maintainer already said lgtm subject to screenshot and CI; author supplied a screenshot retaken at the branch tip and explains the subsequent rendering adjustments, and all version build checks pass. No specific blocking defect established in this bounded static review; generic requests to inspect lifecycle or take another screenshot do not justify category2. |

## 2. Leave comments / resolve focused blockers (49)

| PR | Head | Checks / merge state | Reason / next review point |
| --- | --- | --- | --- |
| [PrismarineJS/mineflayer #4062](https://github.com/PrismarineJS/mineflayer/pull/4062) — entities: read the 1.21.2+ entity_teleport layout (velocity, float rotation, relative flags) | `cb6e354d` | SUCCESS; MERGEABLE | Focused correctness fix: relative teleport code handles dx/dy/dz but omits flags.yawDelta (ROTATE_DELTA), despite the codec/source evidence defining it. Add rotated-relative-velocity coverage, and land/release minecraft-data #1273. |
| [PrismarineJS/mineflayer #4063](https://github.com/PrismarineJS/mineflayer/pull/4063) — physics: send the movement input packets the vanilla client sends | `fc480ee8` | SUCCESS; MERGEABLE | The patch has broad scope, but breadth and a preference to split sprint policy from packet parity do not establish a necessary major redesign. The new sprint state and packet order are localized to physics.js and can be reviewed with focused corrections. Concrete review points: require a minecraft-data version carrying sneakUsesEntityAction (or a backward-compatible fallback), since a missing feature now suppresses legacy sneak entity_action packets; reset or resynchronize the added sent-input/sprint state on login/respawn; clarify and cover sprint control with physics disabled and the supported cross-version water/flying rules. These are bounded dependency/state-policy fixes, not evidence that the overall packet/simulation approach must be replaced. |
| [PrismarineJS/mineflayer #4089](https://github.com/PrismarineJS/mineflayer/pull/4089) — Right-click entities like the vanilla client: interact pair, real sneak state, hit point; useOn and mount share the path | `ba08fc07` | FAILURE; CONFLICTING | Resolve conflicts and test the real serializer on old interact/interact_at versus 26.1 location packets. Use named hand enums; do not make this depend on accepting the mapper rollback in data #1278. |
| [PrismarineJS/prismarine-item #184](https://github.com/PrismarineJS/prismarine-item/pull/184) — Add Item.toHashedNotch for 1.21.5+ window_click slot claims | `0e154a4f` | FAILURE; MERGEABLE | The current diff substantially implements the requested design: a generic ProtoDef hash dependency, HashOps types separated from item code, declarative component codec shapes in hashedSlot.json, and a small registry-dependent conversion layer. A full item-component registry remains a useful broader maintenance project, but no concrete residual issue here requires that architectural prerequisite. Remaining work is bounded: release/pin the hash dependency, reconcile anonymous composite hash bodies used by writeMap (e.g. custom_model_data floats/list fields and the container list element) with node-protodef #177 now requiring named body types, guard null nested slots in the container converter, and run version coverage. These are focused integration/correctness fixes, not grounds for a big refactor classification. |
| [PrismarineJS/mineflayer #4027](https://github.com/PrismarineJS/mineflayer/pull/4027) — inventory: claim clicked slots by hash on 1.21.5+ | `63bada46` | FAILURE; MERGEABLE | Still calls Item.toHashedNotch from open prismarine-item #184 without changing the dependency requirement. Current modern-version checks fail. Release the upstream API, bump the minimum dependency, and rerun; no broader redesign is needed. |
| [ProtoDef-io/node-protodef #177](https://github.com/ProtoDef-io/node-protodef/pull/177) — Add the hash datatype | `dde6ce75` | SUCCESS; MERGEABLE | Earlier compiler requests have been implemented: one-way sizeOf-to-write context, named bodies and CRC32C-only contract. Focused review of digest-type restrictions remains: constant size >=4 bytes permits float output and can lose digest bits. Check interpreter/compiler agreement and signed conversion; update stale PR description. No wholesale redesign established. |
| [ProtoDef-io/ProtoDef #65](https://github.com/ProtoDef-io/ProtoDef/pull/65) — Add the hash datatype | `a2b1cd2d` | NO CHECKS; MERGEABLE | Current spec already explicitly restricts algorithms to crc32c and body to a named type. Review with implementation #177 and tighten digest representation: constant size >=4 bytes alone permits lossy floating-point types or incompatible fixed types; specify exact integer representations and signed behavior. Stale PR body still advertises crypto algorithms. |
| [PrismarineJS/minecraft-data #1283](https://github.com/PrismarineJS/minecraft-data/pull/1283) — Regenerate pc 1.10, 1.11 and 1.12 recipes from the game | `2e266c19` | SUCCESS; MERGEABLE | Review with generator #81 and require reproducible output, including mixed metadata ingredients. The API omitted the three large recipes.json patches, so I have not audited the complete generated diff. |
| [PrismarineJS/minecraft-data-generator #81](https://github.com/PrismarineJS/minecraft-data-generator/pull/81) — Generate recipes for 1.10.2, 1.11.2 and 1.12.2 | `07febf48` | SUCCESS; MERGEABLE | Ask for reproducible generation and crafting checks covering wildcard metadata, mixed alternatives, and intentionally omitted dynamic recipes. This needs domain validation, not a new architecture. |
| [PrismarineJS/mineflayer #4064](https://github.com/PrismarineJS/mineflayer/pull/4064) — health: send player_loaded when the vanilla client would | `1fc5227b` | SUCCESS; MERGEABLE | Request transfer/respawn coverage for the player_loaded state machine. In particular, verify that chunksAnnounced from the previous world cannot satisfy readiness in the next world. |
| [PrismarineJS/mineflayer #4085](https://github.com/PrismarineJS/mineflayer/pull/4085) — use_item and use_item_on like the vanilla client: shared prediction sequence, cursor on the clicked face, no empty-hand use, swing after the packet | `fa678c81` | SUCCESS; MERGEABLE | Resolve the existing counter ownership/plugin review, isolate unrelated interaction behavior where useful, and test sequencing through the serializer, including boat placement. The shared counter already exists in this PR; I would not demand a large new framework. |
| [PrismarineJS/mineflayer #4086](https://github.com/PrismarineJS/mineflayer/pull/4086) — Send the client_command respawn after win_game with the actionId field | `02bc8ced` | SUCCESS; MERGEABLE | The field-name correction is useful, but actionId: 0 still needs reconciliation with the 26.1 named mapper. Add a serialized 26.1 win_game/respawn regression instead of relying on data #1284. |
| [PrismarineJS/mineflayer #4087](https://github.com/PrismarineJS/mineflayer/pull/4087) — Report resource packs as ACCEPTED, DOWNLOADED, SUCCESSFULLY_LOADED and deny once | `dc290378` | SUCCESS; MERGEABLE | The production fix looks localized. Address the existing test-quality feedback and verify status order and exactly one decline through actual packets on the relevant versions. |
| [PrismarineJS/mineflayer #4093](https://github.com/PrismarineJS/mineflayer/pull/4093) — Dismount by holding sneak for one tick on 1.21.3+ | `9eb574f3` | SUCCESS; MERGEABLE | Modern dismount always resets sneak to false in finally, overriding an already-held sneak control. Preserve prior state and cover rejected waitForTicks/disabled physics. Existing asynchronous API/test review is focused work. |
| [PrismarineJS/mineflayer #4103](https://github.com/PrismarineJS/mineflayer/pull/4103) — Window state across close, re-login and respawn: stateId per window, close_window on respawn, closeWindow(null) | `5d4dab47` | SUCCESS; CONFLICTING | Resolve conflicts and exercise per-window stateId, close, relogin and respawn ordering against the server. Several related edge cases, but no demonstrated need for a major redesign. |
| [PrismarineJS/mineflayer #4104](https://github.com/PrismarineJS/mineflayer/pull/4104) — Accept play-phase resource packs when no resourcePack listener answers them | `217e7f92` | SUCCESS; MERGEABLE | Confirmed local bug: autoAcceptResourcePack reads listenerCount after resourcePack emission. A once listener removes itself before its callback declines, so default acceptance follows the decline. Capture listener/handled state before emission and add that regression. Configuration-phase listener behavior also needs explicit policy. |
| [PrismarineJS/mineflayer #4107](https://github.com/PrismarineJS/mineflayer/pull/4107) — fix: answer pings and teleports at the start of the next tick, in arrival order | `4ac72337` | SUCCESS; CONFLICTING | A concrete ordering bug needs correction: queuing the entire position handler while player_rotation remains immediate means position(yaw A) followed by player_rotation(yaw B) between ticks ends at A instead of B; immediate velocity updates have the same interaction with deferred teleports. However, the shared FIFO for pong and teleport replies remains useful. A focused revision can apply incoming teleport state in arrival order, capture immutable reply values, and defer the outgoing replies, or consistently schedule the relevant state handlers. Add regression coverage for the mixed-packet sequence and explicitly discard old-world queued work on respawn/configuration transitions; resolve the existing merge conflict. This warrants focused blocking review comments, with no demonstrated need for a substantial redesign of the entire physics system. |
| [PrismarineJS/mineflayer #4108](https://github.com/PrismarineJS/mineflayer/pull/4108) — physics: a teleport leaves the standing flag alone | `4f31b3e1` | SUCCESS; MERGEABLE | Narrow state change has a real server/client test proving retained grounded state after teleport with physics disabled. Coordinate immediate teleport-reply flags with #4063 and cover airborne login/teleport independently; current test starts observing after the reply and therefore does not check its flags. |
| [PrismarineJS/mineflayer #4111](https://github.com/PrismarineJS/mineflayer/pull/4111) — Read the scoreboard objective title as a chat component | `4ec9950e` | SUCCESS; MERGEABLE | The conversion belongs in prismarine-chat as implemented, but title changes from string to ChatMessage. Confirm compatibility/release handling and update examples; coordinate overlap with #4112. |
| [PrismarineJS/mineflayer #4112](https://github.com/PrismarineJS/mineflayer/pull/4112) — scoreboard: read the 1.20.3+ objective and score packets | `b5668ef0` | SUCCESS; MERGEABLE | Confirmed distinction lost: reset_score with an explicitly named unknown objective passes undefined into removeScore, which deletes the entity score from every objective. Distinguish missing objective_name from unknown objective; also resolve overlap with #4111 title conversion. |
| [PrismarineJS/mineflayer #4115](https://github.com/PrismarineJS/mineflayer/pull/4115) — placeBlock: derive the face and the cursor from a raycast, like the client does | `27805567` | SUCCESS; MERGEABLE | The current head already preserves and documents legacy fallback (bot.placeFaceStrict defaults false), validates both hit.position and hit.face, and tests actual prismarine-world/chunk raycasts. The PR body still describes unconditional rejection, but docs and implementation supersede it. The strongest bounded correctness question is that aimAtFace computes eye/hit before awaited bot.lookAt and then sends that saved cursor; movement during the await can invalidate the promised correspondence with the bot's actual look. Add moving-during-look and partial-shape coverage as focused revisions. Shared activation/placement architecture is optional cleanup; no major redesign prerequisite is established. |
| [PrismarineJS/mineflayer #4116](https://github.com/PrismarineJS/mineflayer/pull/4116) — waitForTicks: say when it is physics that is missing, not the server | `33f944e4` | SUCCESS; MERGEABLE | Small useful diagnostic change. Address the existing request to exercise it with a real mineflayer/nmp pair; no shared fixture refactor is necessary to do that. |
| [PrismarineJS/mineflayer #4118](https://github.com/PrismarineJS/mineflayer/pull/4118) — Expose the interaction ranges the server sends, and the reach check vanilla makes | `e0f82108` | SUCCESS; MERGEABLE | Unresolved reviewer feedback warrants revisions, but does not establish a substantial redesign prerequisite. extremeheat asks 'Why not inside prismarine-entity ?' on lib/attributes.js (discussion_r3998184615), and requests a node-minecraft-protocol server/client test rather than the fake bot (discussion_r3998183658). The helper totals 38 lines, including unchanged getAttributeValue math moved out of explosion.js; ownership can be resolved by a small upstream extraction plus consumer wiring. Attribute tests currently assign bot.entity.attributes directly and therefore do not verify entity_update_attributes packet decoding. Resolve ownership and exercise that real packet path before landing; describe an upstream dependency conditionally until the reviewer confirms that location is required. Category 3 would overstate the evidence by converting a reviewer question into a mandatory major redesign. |
| [PrismarineJS/mineflayer-pathfinder #376](https://github.com/PrismarineJS/mineflayer-pathfinder/pull/376) — createHuman: cap the wait for the head to settle | `e836c40b` | SUCCESS; MERGEABLE | Clarify whether timeout means best-effort completion or an error: settled() now resolves even if the head never settled. Make public lookAt behavior and tests match that choice. |
| [PrismarineJS/mineflayer-pathfinder #377](https://github.com/PrismarineJS/mineflayer-pathfinder/pull/377) — createHuman: a walkTo for the goal in flight joins that walk instead of superseding it | `021933ef` | SUCCESS; MERGEABLE | Joining a nearby in-flight goal ignores potentially different radius, timeout and other options. Define equivalence before coalescing requests and test differing options. |
| [PrismarineJS/mineflayer-pathfinder #379](https://github.com/PrismarineJS/mineflayer-pathfinder/pull/379) — createHuman: hold the controller through a setback storm | `79bed54c` | SUCCESS; MERGEABLE | Make the default setback policy explicit and test interrupted/in-flight actions. In particular, recheck the hold after lookAt's reaction delay; a pre-delay check alone can miss a new setback. |
| [PrismarineJS/node-minecraft-protocol #1522](https://github.com/PrismarineJS/node-minecraft-protocol/pull/1522) — Match the vanilla client in the login/configuration handshake: cookies, brand, per-login chat session, known packs | `cb12f2a9` | SUCCESS; MERGEABLE | Separate the cookie schema defect from the handshake fix; the empty-buffer workaround only addresses the absent-cookie case. Request non-empty cookie round trips and reconfiguration/chat-session tests, and coordinate settings ownership with mineflayer #4065. |
| [PrismarineJS/node-minecraft-protocol #1526](https://github.com/PrismarineJS/node-minecraft-protocol/pull/1526) — test: select the version under test with MC_VERSION instead of a title grep | `80bc3b87` | FAILURE; MERGEABLE | #1527 is merged, so its code fix is no longer an outstanding dependency. The current head nevertheless retains failures across every version job; update/rerun with merged #1527 and inspect newly enabled suites. The cached patch itself shows a focused, reasonable test-discovery correction, not a new architectural issue. |
| [PrismarineJS/prismarine-item #188](https://github.com/PrismarineJS/prismarine-item/pull/188) — Render an item's custom name in displayName | `45fd22d6` | SUCCESS; MERGEABLE | Use prismarine-chat rather than another partial chat parser: translate components currently become empty text. Define displayName restoration when the custom name is removed and reconcile #189. |
| [PrismarineJS/prismarine-item #189](https://github.com/PrismarineJS/prismarine-item/pull/189) — Read the custom name and lore components as the JSON string they used to be | `7cb3fc65` | SUCCESS; MERGEABLE | Check the full component conversion rather than only nbt.simplify plus JSON.stringify: byte booleans and modern component shapes need correct JSON semantics. Test getter/setter round trips and the promised lore return type. |
| [ProtoDef-io/node-protodef #179](https://github.com/ProtoDef-io/node-protodef/pull/179) — Throttle repeated packet-parse error logs in FullPacketParser | `3ce26e3d` | SUCCESS; MERGEABLE | The throttle groups all size mismatches under one key and errors by their first line, which can hide distinct faults. Ask for an intentional bounded grouping policy and tests distinguishing different malformed packets. |
| [ProtoDef-io/node-protodef #180](https://github.com/ProtoDef-io/node-protodef/pull/180) — Serializer: name the packet in write errors from a compiled protocol | `d31ac124` | SUCCESS; MERGEABLE | Follow existing feedback to fix missing compiler error fields, or justify this narrower wrapper separately. The current code assumes a packet.name convention inside generic ProtoDef; this is a concrete review discussion, not grounds to reject better diagnostics. |
| [PrismarineJS/prismarine-viewer #488](https://github.com/PrismarineJS/prismarine-viewer/pull/488) — Render negative-Y sections on 1.18+ worlds | `8049729d` | SUCCESS; MERGEABLE | Negative-Y fix is useful, but world.minY is frozen from a default Chunk rather than column/dimension bounds; direct addColumn callers omitting new arguments still schedule only 0..255. Ask to infer actual bounds and cover the core API. |
| [PrismarineJS/prismarine-viewer #506](https://github.com/PrismarineJS/prismarine-viewer/pull/506) — Render dropped items | `4c250241` | SUCCESS; MERGEABLE | Focused lifecycle bug: getItemMesh returns an empty group then asynchronously adds geometry after image loads, even if the entity was removed and dispose3 already ran. Also an item spawn without stack data is skipped, but a subsequent entityMoved partial update can create the fallback cube before metadata arrives. Request lifecycle/partial-update regression coverage and a narrow fix. |
| [PrismarineJS/prismarine-viewer #513](https://github.com/PrismarineJS/prismarine-viewer/pull/513) — Unload chunks the server unloads, and follow the world across a transfer | `814b753c` | SUCCESS; MERGEABLE | Confirmed against full head file: loadChunk awaits getColumnAt and then emits unconditionally. If unload or world replacement occurs while it waits, the late old-world result republishes the unloaded column. Add an invalidation/generation check and focused regression coverage. |
| [PrismarineJS/prismarine-viewer #514](https://github.com/PrismarineJS/prismarine-viewer/pull/514) — Draw nothing for an invisible entity, like the vanilla client | `45f7de5c` | SUCCESS; MERGEABLE | Confirmed against full head file: invisible removes the entity from the mesh map, but entityMoved sends pos without invisible. update then recreates a fallback cube because the partial update passes the invisible check. Preserve visibility across partial updates; focused change. |
| [PrismarineJS/mineflayer #4065](https://github.com/PrismarineJS/mineflayer/pull/4065) — Send Client Information once, when the vanilla client does | `b59072e9` | SUCCESS; MERGEABLE | Resolve your existing objection to the clientSettings/options interface and verify initialization order with nmp #1522. This is a targeted connection-settings contract decision. |
| [PrismarineJS/prismarine-viewer #503](https://github.com/PrismarineJS/prismarine-viewer/pull/503) — Add viewer.waitForReady() for a fully textured first frame | `af9075bb` | SUCCESS; CONFLICTING | Conflicting stack must be rebased. The readiness promise resolves on an atlas load failure (null is swallowed) and lazy entity textures only begin loading on first render, so the stated fully textured first-frame contract needs narrowing or error/lazy-texture handling. No broader architecture change is necessary. |
| [PrismarineJS/prismarine-viewer #484](https://github.com/PrismarineJS/prismarine-viewer/pull/484) — Add Minecraft 26.1 support | `dc777aa0` | FAILURE; MERGEABLE | The 1.14.4 version build still fails while 26.1 succeeds. Resolve the existing maintainer request to release dependencies and rerun, and establish the cause of the old-version failure before merging. |
| [PrismarineJS/mineflayer #4074](https://github.com/PrismarineJS/mineflayer/pull/4074) — test: gate the no-chunk physics check on a chunk load instead of a 2 s sleep | `3a78e927` | FAILURE; MERGEABLE | Useful no-chunk test improvement, but attach event waits before triggering chunk delivery and obtain passing checks or demonstrate failures against the same base revision. |
| [PrismarineJS/mineflayer #4075](https://github.com/PrismarineJS/mineflayer/pull/4075) — test: build only the versions under test | `46c13151` | FAILURE; MERGEABLE | Code looks suitable for a small test-infrastructure merge; current checks fail. Establish baseline equivalence or get a passing rerun rather than treating the author's flake diagnosis as established. |
| [PrismarineJS/mineflayer #4081](https://github.com/PrismarineJS/mineflayer/pull/4081) — fix: give teamRemoved the team that was removed | `f021eb8a` | FAILURE; MERGEABLE | The production fix is straightforward: emit the saved team object after deletion. Resolve existing fixture/data feedback and failing checks; no large refactor needed. |
| [PrismarineJS/mineflayer #4099](https://github.com/PrismarineJS/mineflayer/pull/4099) — fix: cancel the deferred respawn reply once it is stale, and write no movement packets outside play | `05563854` | FAILURE; MERGEABLE | Useful lifecycle fix, but current checks fail. Verify cancellation on newer teleport/configuration/login/end, and coordinate with the queue changes in #4107. |
| [PrismarineJS/mineflayer #4094](https://github.com/PrismarineJS/mineflayer/pull/4094) — Swing every tick while digging, skip STOP on instant breaks and abort with face DOWN | `36e6265f` | SUCCESS; MERGEABLE | Review after #4085; rebase away inherited changes and verify start/stop/abort/swing ordering through real packets, especially instant breaks and cancellation. |
| [PrismarineJS/prismarine-viewer #510](https://github.com/PrismarineJS/prismarine-viewer/pull/510) — Decode host images without holding the event loop | `3bbee612` | SUCCESS; MERGEABLE | Current base is still player-skins (#490), which remains open; merged #501 does not remove that stack dependency. The production change is focused and checks pass. Land/retarget the base and rerun; the single-sample event-loop timing comparison also deserves a small robustness adjustment. |
| [PrismarineJS/prismarine-viewer #511](https://github.com/PrismarineJS/prismarine-viewer/pull/511) — Parse the block states in the mesher, not on the calling thread | `05bd6ecb` | SUCCESS; MERGEABLE | Current base remains host-6-async-png (#510), itself based on open #490. Land/retarget the stack and rerun. The implementation and text-to-mesher coverage are focused; loadText is already documented in viewer/README.md and host/index.js, so documentation is not an outstanding blocker. |
| [PrismarineJS/node-minecraft-protocol #1525](https://github.com/PrismarineJS/node-minecraft-protocol/pull/1525) — fix: send a command with no signable argument as chat_command | `1aca229d` | SUCCESS; MERGEABLE | Packet selection is focused, but the new unsigned path still unconditionally clears _lastSeenMessages.pending although that packet carries no acknowledgement fields. Preserve pending acknowledgement state and cover an unsigned command followed by signed chat. #1527 has merged; #1526 remains relevant because the added suite is excluded by current CI title filtering. |
| [PrismarineJS/mineflayer #4114](https://github.com/PrismarineJS/mineflayer/pull/4114) — placeEntity: write a complete use_item when placing a boat | `b56eced2` | SUCCESS; MERGEABLE | Tests already serialize every packet through real minecraft-protocol, so missing serialization is not a valid objection. Address the existing request for an actual nmp server/client test and coordinate the boat sequence with shared counter #4085 (current production sequence remains0). |
| [PrismarineJS/prismarine-physics #142](https://github.com/PrismarineJS/prismarine-physics/pull/142) — Obey the flight the server grants in the abilities packet | `73252f2c` | SUCCESS; MERGEABLE | Focused flight correction still needs vertical-input behavior: the current simulatePlayer jump/sneak branch has no flying case, so an airborne flying bot with zero vertical velocity cannot ascend using jump. Add coverage for flight input and water/ground transitions; existing tests only cover hovering, damping and horizontal acceleration. |

## 3. Requires substantial changes / refactoring (2)

| PR | Head | Checks / merge state | Reason / next review point |
| --- | --- | --- | --- |
| [PrismarineJS/mineflayer #4021](https://github.com/PrismarineJS/mineflayer/pull/4021) — placeBlock: reject an occupied target up front; nether test clears its sign spot | `4a16956f` | SUCCESS; MERGEABLE | Keep category 3: the current plugin owns two hard-coded replaceability tables and its own tags listener. The unresolved maintainer request is a concrete ownership change: represent replaceability in versioned minecraft-data block metadata, let prismarine-registry update that property from server tags, and consume the resulting property in placeBlock. That requires a shared data/schema and registry contract plus version/reload coverage, not just extracting the arrays into a helper. The nether fixture cleanup itself is independently small; the production guard is what needs restructuring. |
| [PrismarineJS/mineflayer-pathfinder #382](https://github.com/PrismarineJS/mineflayer-pathfinder/pull/382) — createHuman: bridge over gaps the way a player does | `c59be20d` | FAILURE; MERGEABLE | Keep category 3: bridgeTo adds a separate async movement controller with no registered action identity. It cancels an existing walk once but cannot be superseded safely by another bridge/walk, checks human.active only between complete bridge steps, and toLip/stepOnto await bare physicsTick promises that can outlive their wall-clock deadlines indefinitely. Required repair is to integrate bridgeTo and standalone placeAhead with the existing controller lifecycle: shared operation ownership/cancellation across walk, bridge and stop; cancellation/deadline-aware waits through equip, aiming, lip movement and stepping; and guarded finally cleanup of head/forward/sneak controls so stale operations cannot move the bot or release a successor controls. This alters execution/ownership throughout the new feature, rather than adding an isolated timeout guard. Add stop-mid-step, supersession and physics-disabled tests. |

## 4. Reject the current approach (3)

| PR | Head | Checks / merge state | Reason / next review point |
| --- | --- | --- | --- |
| [PrismarineJS/minecraft-data #1278](https://github.com/PrismarineJS/minecraft-data/pull/1278) — Fix pc/26.1 use_entity hand: plain varint, not a mapper | `b3eb1d49` | FAILURE; MERGEABLE | Recommend rejecting this mapper-removal direction: the entire diff replaces the named hand enum with varint, and the author reports mineflayer has already adapted and explicitly offers making the other hand fields mappers instead. A reversal would replace the proposed solution, rather than repair it. No maintainer comment on this PR establishes rejection or consensus; this recommendation extends the named-enum preference stated on #1284. Failing CI is a separate, unresolved blocker. |
| [PrismarineJS/minecraft-data #1284](https://github.com/PrismarineJS/minecraft-data/pull/1284) — Fix pc/26.1 client_command actionId: plain varint, not a mapper | `2c7d1074` | SUCCESS; MERGEABLE | Recommend rejecting removal of the actionId mapper. The diff directly opposes extremeheat's explicit preference here to eliminate numerical enum datatypes and retain stable names across version-dependent ordinal changes. Adapt consumers and make version representations consistent instead. This is supported by one maintainer's stated direction, not an explicit rejection decision or demonstrated project-wide consensus; passing CI does not resolve that direction disagreement. |
| [PrismarineJS/minecraft-data #1292](https://github.com/PrismarineJS/minecraft-data/pull/1292) — Revert the 1.21.6 enum mappers whose ordinals never moved | `8f96413d` | SUCCESS; MERGEABLE | Recommend rejecting the broad rollback of four named-enum fields. Stable historical ordinals do not justify a second API migration after consumers adopt names; retain named representations and address compatibility deliberately. The diff also changes difficulty/newDifficulty from varint to u8, despite discussion identifying vanilla varint encoding; correct wire types separately. The comments on this PR do not explicitly reject mapper removal: category 4 is our assessment informed by #1284's named-enum preference, not maintainer consensus. Fixing the wire-type issue alone would leave the direction objection intact. |

## Already merged; excluded from counts

- [PrismarineJS/mineflayer #4083](https://github.com/PrismarineJS/mineflayer/pull/4083): Track the abilities the server grants
- [PrismarineJS/mineflayer #4067](https://github.com/PrismarineJS/mineflayer/pull/4067): test(nether): let retries pass by respecting the player's portal cooldown
- [PrismarineJS/mineflayer #4073](https://github.com/PrismarineJS/mineflayer/pull/4073): fix: make bot.controlState's properties enumerable
- [PrismarineJS/mineflayer #4101](https://github.com/PrismarineJS/mineflayer/pull/4101): Throw a descriptive error when bot.chat is called before login
- [PrismarineJS/mineflayer #4102](https://github.com/PrismarineJS/mineflayer/pull/4102): Normalize open_window titles to ChatMessage
- [PrismarineJS/mineflayer-pathfinder #375](https://github.com/PrismarineJS/mineflayer-pathfinder/pull/375): fix: goto rejects an unreachable goal instead of resolving, and stop() no longer latches while idle
- [PrismarineJS/node-minecraft-protocol #1527](https://github.com/PrismarineJS/node-minecraft-protocol/pull/1527): test: give the declare_commands tree the packet's rootIndex
- [PrismarineJS/prismarine-item #185](https://github.com/PrismarineJS/prismarine-item/pull/185): Expose the custom_data component as item.nbt on 1.20.5+
- [PrismarineJS/prismarine-viewer #509](https://github.com/PrismarineJS/prismarine-viewer/pull/509): fix: resolve entity bone parents by name, whatever their case

## Design and rejection recommendations

Use targeted designs only for the category-3 items above. The review does not establish a need to refactor the entire test suite or create a general interaction framework before landing the other PRs.

The three enum rollback recommendations remain category 4 under the meaning “reject this approach.” [minecraft-data #1284](https://github.com/PrismarineJS/minecraft-data/pull/1284) explicitly records a maintainer preference for named enums. Rejection of #1278 and #1292 is our recommendation from that direction, not a claim that maintainers already decided to reject those PRs. The #1278 author offers to reverse the change and make other hand fields named mappers instead.
